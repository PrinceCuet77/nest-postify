enum Role {
  USER
  PREMIUM_USER
  ADMIN
}

enum AuthProvider {
  CREDENTIALS
  GOOGLE
}

enum UserStatus {
  NOT_VERIFIED
  VERIFIED
  SUSPENDED
}

enum PostStatus {
  DRAFT
  PUBLISHED
  PREMIUM
  ARCHIVED
  DELETED
}

enum PaymentStatus {
  PENDING
  SUCCESS
  FAILED
  CANCELLED
  REFUNDED
}
